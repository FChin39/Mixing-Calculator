"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function stringPath(path) {
    return path.reduce((acc, current) => {
        if (typeof current === 'string') {
            if (acc) {
                acc += '.' + current;
            }
            else {
                acc += current;
            }
        }
        else {
            acc += '[' + current + ']';
        }
        return acc;
    }, '');
}
exports.default = stringPath;
