import { NodePath } from '@babel/traverse';
import { RenderNode, Options } from '../types';
/**
 * 创建单个原生模板
 *
 * @param element
 * @param {NodePath} path
 * @param {string} module
 * @param {(Array<string | number>)} dataPath
 * @returns {string}
 */
export declare function createTemplate(element: RenderNode, path: NodePath<any>, dataPath: Array<string | number>, options: Options): string;
