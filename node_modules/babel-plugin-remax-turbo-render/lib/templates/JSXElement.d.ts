import * as t from '@babel/types';
import { NodePath } from '@babel/traverse';
import { RenderNode, Options } from '../types';
/**
 *  创建 JSXElement 对应的模板
 *
 * @export
 * @param {RenderNode} node
 * @param {NodePath} path
 * @param {(Array<string | number>)} dataPath
 * @param {Function} createTemplate
 * @returns
 */
export default function JSXElement(node: RenderNode<t.JSXElement>, path: NodePath, dataPath: Array<string | number>, createTemplate: (...args: any[]) => void, options: Options): string;
