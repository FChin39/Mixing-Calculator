import * as t from '@babel/types';
/**
 * 创建 JSXExpressionContainer 对应的模板
 *
 * @export
 * @param {t.JSXExpressionContainer} element
 * @param {(Array<string | number>)} dataPath
 * @returns
 */
export default function JSXExpressionContainer(element: t.JSXExpressionContainer, dataPath: Array<string | number>): string;
