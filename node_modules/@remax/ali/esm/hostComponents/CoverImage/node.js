import { unique } from '@remax/shared';
export var alias = {
    id: 'id',
    className: 'class',
    src: 'src',
    style: 'style',
    onClick: 'onTap',
    onTap: 'onTap',
};
export var props = unique(Object.values(alias));
