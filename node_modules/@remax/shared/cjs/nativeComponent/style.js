"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.walk = void 0;
var fs_1 = require("fs");
var postcss_1 = __importDefault(require("postcss"));
var helpers_1 = require("./helpers");
var walk = function (filePath, cssPaths) {
    if (!fs_1.existsSync(filePath)) {
        return;
    }
    cssPaths.add(filePath);
    var content = fs_1.readFileSync(filePath);
    var ast = postcss_1.default.parse(content);
    if (!ast.nodes) {
        return;
    }
    ast.nodes.forEach(function (node) {
        if (node.type === 'atrule' && node.name === 'import') {
            var file = helpers_1.getPath(filePath, node.params.replace(/'|"/g, ''));
            if (!fs_1.existsSync(file)) {
                console.error("\u6587\u4EF6 " + file + " \u4E0D\u5B58\u5728");
                return;
            }
            exports.walk(file, cssPaths);
        }
    });
};
exports.walk = walk;
function style(platformConfig, id) {
    var cssPaths = new Set();
    var filePath = id.replace(/\.js$/, platformConfig.style);
    exports.walk(filePath, cssPaths);
    return Array.from(cssPaths);
}
exports.default = style;
