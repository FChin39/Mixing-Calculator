var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
import jsHelper from './jsHelper';
import jsModule from './modules';
import style from './style';
import json from './json';
import template from './template';
import usingComponents from './usingComponents';
export default function getAssets(platformConfig, resourcePath, options) {
    var assets = __spreadArrays(jsModule(options, resourcePath), jsHelper(platformConfig, resourcePath), style(platformConfig, resourcePath), json(resourcePath), template(platformConfig, options, resourcePath), usingComponents(resourcePath, options).reduce(function (acc, id) { return __spreadArrays(acc, getAssets(platformConfig, id, options)); }, []));
    return assets;
}
