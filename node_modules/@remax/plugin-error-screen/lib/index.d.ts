declare const _default: (_: any, { cwd, rootDir }: {
    cwd: string;
    rootDir: string;
}) => {
    configWebpack({ config }: {
        config: any;
    }): void;
    registerRuntimePlugin(): string;
};
export default _default;
