import { compilation } from 'webpack';
import Builder from '../Builder';
import NormalEntry from './NormalEntry';
import webpack from 'webpack';
export default class VirtualEntry extends NormalEntry {
    originalSource: string;
    virtualPath: string;
    virtualModule: any;
    constructor(builder: Builder, name: string, filename: string);
    outputSource(): string;
    updateSource(force?: boolean): void;
    addToWebpack(compiler: webpack.Compiler, compilation: compilation.Compilation): Promise<unknown>;
}
