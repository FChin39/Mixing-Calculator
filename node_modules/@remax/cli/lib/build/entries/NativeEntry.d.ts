/// <reference types="node" />
import { loader } from 'webpack';
import VirtualEntry from './VirtualEntry';
import Builder from '../Builder';
import NativeAssets from '../NativeAssets';
export default class NativeEntry extends VirtualEntry {
    dependentEntries: NativeEntry[];
    assets: NativeAssets;
    constructor(builder: Builder, name: string, filename: string);
    getManifest(): {
        usingComponents: Record<string, string>;
    };
    getDependentEntries(): Map<string, NativeEntry>;
    getAssets(): Map<string, {
        output: string;
        content: Buffer;
        filename: string;
    }>;
    watchAssets(loaderContext: loader.LoaderContext): void;
    get rawManifestFile(): string;
    private readRawManifest;
}
