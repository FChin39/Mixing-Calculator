import type { Options } from '@remax/types';
import * as webpack from 'webpack';
import API from '../API';
export declare function run(options: Options, api: API): webpack.Compiler;
export declare function buildApp(options: Options): webpack.Compiler;
export declare function internalBuildApp(options: Options, api: API): webpack.Compiler;
export declare function buildMiniPlugin(options: Options): any;
