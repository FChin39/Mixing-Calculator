import { compilation } from 'webpack';
import type { Options, Meta, EntryInfo } from '@remax/types';
import API from '../../../../API';
export default function createTurboTemplate(api: API, options: Options, page: EntryInfo, modules: string[], meta: Meta, compilation: compilation.Compilation): Promise<void>;
