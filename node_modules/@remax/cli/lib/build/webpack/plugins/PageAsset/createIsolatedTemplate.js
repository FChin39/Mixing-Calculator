"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const ejs_1 = __importDefault(require("ejs"));
const build_store_1 = __importDefault(require("@remax/build-store"));
async function createIsolatedTemplate(meta, compilation) {
    const templates = [];
    build_store_1.default.extractedTemplates.forEach(t => {
        if (t.isolated !== false) {
            templates.push(t);
        }
    });
    let source = await ejs_1.default.renderFile(meta.staticEjs.isolatedTemplates, {
        templates,
    });
    // 空文件会被 webpack 忽略掉
    if (source.trim() === '') {
        source = '<!-- empty -->\n';
    }
    const fileName = `isolated${meta.template.extension}`;
    compilation.assets[fileName] = {
        source: () => source,
        size: () => Buffer.byteLength(source),
    };
}
exports.default = createIsolatedTemplate;
