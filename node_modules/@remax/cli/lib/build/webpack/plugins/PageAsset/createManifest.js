"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const path = __importStar(require("path"));
const getUsingComponents_1 = require("../getUsingComponents");
function createManifest(builder, page, compilation, cache) {
    const { options, api } = builder;
    const manifestPath = page.name + '.json';
    const config = page.getManifest();
    const prefixPath = path.relative(path.join('./', options.output, path.dirname(manifestPath)), path.join('./', options.output));
    const usingComponents = {};
    getUsingComponents_1.getUsingComponents(page.filename, compilation, options, prefixPath).forEach(component => {
        usingComponents[component.id] = component.path;
    });
    config.usingComponents = Object.assign(Object.assign({}, (config.usingComponents || {})), usingComponents);
    const source = JSON.stringify(api.onPageConfig({
        page: page.name,
        config,
    }), null, 2);
    cache.invalid(manifestPath, source, () => {
        compilation.assets[manifestPath] = {
            source: () => source,
            size: () => Buffer.byteLength(source),
        };
    });
}
exports.default = createManifest;
