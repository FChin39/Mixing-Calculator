import { compilation } from 'webpack';
import type { Options, Meta, EntryInfo } from '@remax/types';
import SourceCache from '../../../../SourceCache';
import API from '../../../../API';
import NormalEntry from '../../../entries/NormalEntry';
export declare function createRenderOptions(page: string, compilation: compilation.Compilation, options: Options, filter?: boolean): {
    components: Map<string, import("@remax/types").ComponentManifest>;
    skipComponents: string[];
    slotView: {
        props: string[];
    };
};
export default function createPageTemplate(api: API, options: Options, page: EntryInfo, meta: Meta, compilation: compilation.Compilation, cache: SourceCache): Promise<void>;
export declare function createBaseTemplate(pages: Map<string, NormalEntry>, options: Options, meta: Meta, compilation: compilation.Compilation, cache: SourceCache): Promise<null | undefined>;
