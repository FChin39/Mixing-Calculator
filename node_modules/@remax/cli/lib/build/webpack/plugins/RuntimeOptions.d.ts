import { Compiler, compilation } from 'webpack';
import Builder from '../../Builder';
declare type Events = Set<string>;
export declare const pageClassEvents: Map<string, Events>;
export declare const appClassEvents: Map<string, Events>;
export default class RuntimeOptionsPlugin {
    builder: Builder;
    constructor(builder: Builder);
    apply(compiler: Compiler): void;
    createRuntimeOptions(compilation: compilation.Compilation, hostComponents: any, pageEvents: any, appEvents: any, assetsPath: string): void;
    getPageEvents(chunks: compilation.Chunk[]): any;
    getAppEvents(): string[];
    getHostComponents(): any;
}
export {};
