import { compilation } from 'webpack';
import type { Options, Meta, EntryInfo } from '@remax/types';
import SourceCache from '../../../../SourceCache';
export declare function createRenderOptions(componentPath: string, compilation: compilation.Compilation, options: Options): {
    components: Map<string, import("@remax/types").ComponentManifest>;
    skipComponents: string[];
    slotView: {
        props: string[];
    };
};
export default function createComponentTemplate(component: EntryInfo, options: Options, meta: Meta, compilation: compilation.Compilation, cache: SourceCache): Promise<void>;
