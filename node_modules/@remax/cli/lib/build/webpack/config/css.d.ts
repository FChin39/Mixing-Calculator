import Config from 'webpack-chain';
import Builder from '../../Builder';
export interface RuleConfig {
    name: string;
    test: RegExp;
    loader?: string;
    options?: any;
}
export declare function addCSSRule(webpackConfig: Config, builder: Builder, web: boolean, ruleConfig: RuleConfig): void;
export declare function cssConfig(webpackConfig: Config, builder: Builder, web: boolean): void;
