import * as t from '@babel/types';
import type { Plugin, Meta, Platform, Options } from '@remax/types';
import Config from 'webpack-chain';
import { RuleConfig } from './build/webpack/config/css';
import yargs from 'yargs';
export default class API {
    plugins: Plugin[];
    adapter: {
        name: string;
        target: string;
        packageName: string;
        options: {};
    };
    meta: {
        global: string;
        template: {
            extension: string;
            tag: string;
            src: string;
        };
        style: string;
        jsHelper: {
            extension: string;
            tag: string;
            src: string;
        };
        include: {
            tag: string;
            src: string;
        };
    };
    getMeta(): Meta;
    processProps(componentName: string, props: string[], additional?: boolean, node?: t.JSXElement): string[];
    onBuildStart(config: Options): void;
    onAppConfig(config: any): any;
    onPageConfig({ page, config }: {
        page: string;
        config: any;
    }): any;
    configWebpack(params: {
        config: Config;
        webpack: any;
        addCSSRule: (ruleConfig: RuleConfig) => void;
    }): void;
    configBabel(params: {
        config: any;
    }): void;
    extendCLI(cli: yargs.Argv): void;
    onPageTemplate({ template, page }: {
        template: string;
        page: string;
    }): string;
    getRuntimePluginFiles(): (string | undefined)[];
    loadBuiltinPlugins(options: Options): void;
    registerAdapterPlugins(targetName: Platform): void;
    registerPlugins(plugins: Plugin[]): void;
    private registerHostComponents;
}
