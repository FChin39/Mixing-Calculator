var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { RuntimeOptions, PluginDriver } from '@remax/framework-shared';
import { CacheRoute, CacheSwitch } from '@remax/react-router-cache-route';
import { Router, Route, Redirect, useLocation } from 'react-router-dom';
import loadable from '@loadable/component';
import createAppConfig from './createAppConfig';
import createPageConfig from './createPageConfig';
import { TabBar } from './TabBar';
export default function createApp(options, history) {
    var _a, _b, _c;
    var _d = options.async, async = _d === void 0 ? true : _d, appComponent = options.appComponent, appConfig = options.appConfig, pageComponents = options.pageComponents, pages = options.pages, _e = options.plugins, plugins = _e === void 0 ? [] : _e;
    var AppConfig = createAppConfig(appComponent);
    var pluginDriver = new PluginDriver(plugins.map(function (plugin) { return plugin.default || plugin; }));
    RuntimeOptions.apply({ pluginDriver: pluginDriver });
    var pageComponentsHoc = pages.map(function (page, i) {
        var pageComponent = async
            ? loadable(function () {
                return pageComponents[i]().then(function (_a) {
                    var c = _a.default;
                    return createPageConfig(c, page.route);
                });
            })
            : createPageConfig(pageComponents[i](), page.route);
        return pageComponent;
    });
    return (React.createElement(Router, { history: history },
        React.createElement(AppConfig, null,
            React.createElement(CacheSwitch, null,
                React.createElement(Route, { exact: true, path: "/" },
                    React.createElement(Redirect, { to: "/" + ((_a = pages[0]) === null || _a === void 0 ? void 0 : _a.route) })),
                pages.map(function (page, i) {
                    return (React.createElement(CacheRoute, { key: page.route, className: "remax-cached-router-wrapper", path: "/" + page.route, exact: true }, function (props) {
                        var pageComponent = pageComponentsHoc[i];
                        return React.createElement(pageComponent, __assign(__assign({}, props), { pageConfig: __assign(__assign({}, appConfig.window), page.config), tabBar: appConfig.tabBar }));
                    }));
                }),
                React.createElement(Route, null,
                    React.createElement(Redirect, { to: "/" + ((_b = pages[0]) === null || _b === void 0 ? void 0 : _b.route) }))),
            ((_c = appConfig.tabBar) === null || _c === void 0 ? void 0 : _c.items) && appConfig.tabBar.items.length > 0 && (React.createElement(TabBar, { history: history, config: appConfig.tabBar })),
            process.env.NODE_ENV === 'development' && React.createElement(LogLocation, null))));
}
function LogLocation() {
    var location = useLocation();
    React.useEffect(function () {
        console.log('[remax][react-router]location change', location.pathname);
    }, [location]);
    return null;
}
