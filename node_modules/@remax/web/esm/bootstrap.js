import { render } from 'react-dom';
import hd from 'umi-hd';
import { createHashHistory, createBrowserHistory } from 'history';
import { RuntimeOptions } from '@remax/framework-shared';
import createApp from './createApp';
export default function bootstrap(options) {
    hd();
    var appConfig = options.appConfig;
    var history = !appConfig.router || appConfig.router.type !== 'browser' ? createHashHistory() : createBrowserHistory();
    RuntimeOptions.apply({ history: history, mpa: false });
    render(createApp(options, history), document.getElementById('remax-app'));
}
