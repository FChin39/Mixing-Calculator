"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.decode = exports.getLiteralText = void 0;
var cache = {};
function getLiteralText(text) {
    if (cache[text]) {
        return cache[text];
    }
    var el = document.createElement('div');
    el.innerHTML = text;
    cache[text] = el.textContent;
    return cache[text];
}
exports.getLiteralText = getLiteralText;
var keys = ['&nbsp;', '&lt;', '&gt;', '&amp;', '&apos;', '&ensp;', '&emsp;'];
var replaceEncodeReg = new RegExp(keys.join('|'), 'g');
function decode(str) {
    return str.replace(replaceEncodeReg, function (m) { return getLiteralText(m); });
}
exports.decode = decode;
