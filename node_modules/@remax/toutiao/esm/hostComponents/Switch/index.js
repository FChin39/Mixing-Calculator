import { createHostComponent } from '@remax/runtime';
export var Switch = createHostComponent('switch');
Switch.defaultProps = {
    checked: false,
    disabled: false,
    type: 'switch',
    color: '#F85959',
};
