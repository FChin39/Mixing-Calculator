import { createHostComponent } from '@remax/runtime';
export var Video = createHostComponent('video');
Video.defaultProps = {
    autoplay: false,
    loop: false,
    showFullscreenBtn: true,
    showPlayBtn: true,
    controls: true,
    objectFit: 'contain',
    playBtnPosition: 'center',
};
