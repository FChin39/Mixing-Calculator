import type { Platform } from '@remax/types';
interface Options {
    target: Platform;
    hostComponents: Map<string, any>;
    skipHostComponents: string[];
    skipProps: string[];
    includeProps: string[];
}
export default function hostComponent(options: Options): any;
export {};
